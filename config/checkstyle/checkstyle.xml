<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="error"/>

    <module name="TreeWalker">

        <!-- NOMMAGE -->
        <module name="TypeName"/>
        <module name="MethodName"/>
        <module name="ConstantName"/>
        <module name="LocalVariableName"/>
        <module name="MemberName"/>

        <!-- JAVADOC -->
        <module name="MissingJavadocMethod">
            <property name="scope" value="public"/>
            <!-- Accesseurs / equals / hashCode / toString générés par les records -->
            <property name="excludeMethodNames"
                      value="^(equals|hashCode|toString|customerId|total|createdAt|productId|quantity|unitPrice)$"/>
        </module>
        <module name="JavadocMethod">
            <property name="accessModifiers" value="public"/>
        </module>
        <module name="MissingJavadocType">
            <property name="scope" value="public"/>
        </module>

        <!-- MISE EN FORME -->
        <module name="LineLength">
            <property name="max" value="100"/>
        </module>
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
        </module>
        <module name="MethodLength">
            <property name="max" value="40"/>
        </module>

        <!-- COMPLEXITÉ -->
        <module name="CyclomaticComplexity">
            <property name="max" value="10"/>
        </module>

        <!-- IMPORTS -->
        <module name="AvoidStarImport"/>
        <module name="UnusedImports"/>

        <!-- NOMBRES MAGIQUES -->
        <module name="MagicNumber">
            <property name="ignoreNumbers" value="-1, 0, 1, 2"/>
            <property name="ignoreAnnotation" value="true"/>
        </module>

        <!-- SUPPRESSIONS PAR COMMENTAIRE -->
        <module name="SuppressionCommentFilter">
            <property name="offCommentFormat" value="CHECKSTYLE.OFF: (\w+)"/>
            <property name="onCommentFormat" value="CHECKSTYLE.ON: (\w+)"/>
        </module>

    </module>
</module>
